const wordsRouter = require("./wordsRoute");
const authRouter = require("./auth");
const statisticsRouter = require("./statistics");
const sendMessageRouter = require("./sendMessage");

module.exports = {
  wordsRouter,
  authRouter,
  statisticsRouter,
  sendMessageRouter,
};
